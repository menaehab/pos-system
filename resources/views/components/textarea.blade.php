<textarea name="{{ $name }}" id="{{ $name }}" wire:model="{{ $name }}"
    placeholder="{{ $placeholder }}"
    class="w-full p-2 border rounded mt-4 focus:outline-2 focus:-outline-offset-2 focus:outline-blue-500">
</textarea>
