<div>
    <x-edit-modal title="{{ __('keywords.edit_supplier') }}" action="updateSupplier">
        <x-text-input name="name" placeholder="{{ __('keywords.name') }}" />
        <x-text-input name="phone" placeholder="{{ __('keywords.phone') }}" />
    </x-edit-modal>
</div>
